-- ProbTT without semicolons (layout-sensitive)

id : A → A
id x = x

const : A → B → A
const x y = x

swap : A × B → B × A
swap p = (snd p , fst p)

compose : (A → B) → (B → C) → A → C
compose f g x = g (f x)

unit : ⊤
unit = ()
